1-car_purchase_amount_model.keras: The trained neural network model for predicting the car purchase amount.
2-scaler.pkl: The pre-fitted StandardScaler used for normalizing input data.
3-app.py: The Streamlit application script for interacting with the model.

4-You have to install the dependencies using pip:

pip install streamlit keras tensorflow joblib numpy scikit-learn streamlit

5- To run this project:
streamlit run "C:/Users/HP/Desktop/temp.py"